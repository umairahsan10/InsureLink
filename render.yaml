services:
  - type: web
    name: insurelink-server
    runtime: node
    rootDir: server
    buildCommand: npm install --include=dev && npx prisma generate && npm run build
    startCommand: node dist/main.js
