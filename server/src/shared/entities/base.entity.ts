// Base Entity
// Common fields for all database entities (id, createdAt, updatedAt, etc.)
export class BaseEntity {
  // id: string;
  // createdAt: Date;
  // updatedAt: Date;
  // Add common entity fields as needed
}
